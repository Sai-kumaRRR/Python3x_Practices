# functions
# Block of code - > which can executed
# they can return something
# they can't return -> no  return
# they have parameters
# They don't parameters / arguments
# Define -> call

def say_hello(name):  # no return type and with arguments
    print("Hello", name)
    say_hello("SAI")

def say_hello_args(name, age):  # no return type and with arguments
    print("Hello", name)
    say_hello_args("Sai", 26)
    say_hello_args("Amit"
    age: 34)

def say_hello_arg_default(name:"Sai"):  # no return type
    print("Hello", name)
    say_hello_arg_default("Amit")
