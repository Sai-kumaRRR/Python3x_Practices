class password:

    def __init__(self, password):
        self._password = password

        def get_password(self, isAuth):
            if isAuth:
                return self._password()
            else:
                print("error")

                def set_password(self, password):
                    if len(password) > 10:
                        self_password = password
                    else:
                        print("weak password")

                        def print_len(self):
                            print("your password len is", len(self._password))

                            pwd = password("hack123")
                            pwd.public_var
                            pwd.print_len()
                            pwd.get_password(False)
                            print(pwd)
                            pwd.set_password("Sai123")
                            pwd.print_len()
